import React, {useState} from 'react'


const Selectors = (boxSelect) => {
    const [boxSelect, setBoxSelect] = useState([])

    return (
        <>
            
        </>
    )
}

export default Selectors
